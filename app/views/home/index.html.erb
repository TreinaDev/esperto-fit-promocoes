<% if user_signed_in? %>
  <div class="container d-flex h-50">
    <div class="row align-self-center w-100">
      <div class="col-8 mx-auto">
        <div class="row justify-content-center align-self-center">
          <div class="btn-group-vertical">
            <%= link_to 'Promoções', promotions_path, :class => "btn btn-outline-dark btn-lg" %>
            <%= link_to 'Cupons avulsos', single_coupons_path, :class => "btn btn-outline-dark btn-lg" %>
            <%= link_to 'Empresas Parceiras', partner_companies_path, :class => "btn btn-outline-dark btn-lg" %>
          </div>
        </div>
      </div>
    </div>
  </div>
<% else %>
  <div class="container d-flex h-50">
    <div class="row align-self-center w-100">
      <div class="col-8 mx-auto">
        <div class="row justify-content-center align-self-center mt-5">
          <h3> Bem-vindo a EspertoFit. Faça login para navegar. </h3>
        </div>
      </div>
    </div>
  </div>
<% end %>
